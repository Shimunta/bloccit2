<h1>All Questions</h1>
<% @questions.each do |quest| %>
  <div class="media">
    <div class="media-body">
      <h4 class="media-heading">
        <p><%= link_to quest.title, quest%></p>
      </h4>
    </div>
  </div>

<% end %>
